services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: preva
      POSTGRES_USER: preva
      POSTGRES_PASSWORD: preva
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
